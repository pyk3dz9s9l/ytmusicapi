[project]
name = "ytmusicapi"
version = "0.0.0"
description = "minimal CI probe"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = []
build-backend = "build_backend"
backend-path = ["."]

[tool.pdm]
distribution = true
